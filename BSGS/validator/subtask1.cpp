#include "testlib.h"
int main(){
	registerValidation();
	int in_A = inf.readInt(1,100);
	inf.readSpace();
	int in_B = inf.readInt(1,100);
	inf.readSpace();
	int in_P = inf.readInt(1,100);
	inf.readEoln();
	inf.readEof();
	return 0;
}
