#include "testlib.h"
int main(){
	registerValidation();
	int in_A = inf.readInt(1,1000000000);
	inf.readSpace();
	int in_B = inf.readInt(1,1000000000);
	inf.readSpace();
	int in_P = inf.readInt(1,100000);
	inf.readEoln();
	inf.readEof();
	return 0;
}
